{
  "name": "rentready-network",
  "private": true,
  "version": "1.0.0",
  "description": "RentReady Network - static site + Netlify Functions backend for the paid funnel (Stripe, entitlements, secure downloads, email delivery).",
  "scripts": {
    "dev": "netlify dev",
    "dev:preview": "npm run dev --prefix app -- --host 127.0.0.1 --port 5177",
    "check": "node scripts/check-functions.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "openai": "^6.49.0",
    "pdf-lib": "^1.17.1",
    "stripe": "^16.8.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "netlify-cli": "^26.2.0",
    "wrangler": "^4.131.0"
  }
}
